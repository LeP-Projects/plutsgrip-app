# Development Environment Configuration

# PostgreSQL Configuration
POSTGRES_USER=plutusgrip_user
POSTGRES_PASSWORD=plutusgrip_password
POSTGRES_DB=plutusgrip_db

# Application Configuration
APP_ENV=development
DEBUG=True

# Security (Development - NOT for production!)
SECRET_KEY=dev-super-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000,http://127.0.0.1:3000,http://127.0.0.1:5173

# pgAdmin Configuration
PGADMIN_EMAIL=admin@plutusgrip.dev
PGADMIN_PASSWORD=admin123

# Database Pool Configuration (Development)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# Logging Configuration
LOG_LEVEL=DEBUG
